import logging
from models import llm_fast, llm_smart

logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)

def analyse_competitors(industry: str, product_summary: str):
    pass